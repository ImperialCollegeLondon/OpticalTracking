# Camera module
The `Camera` class is an enumeration of all available cameras, and the definition of how their data should be turned into a default representation.

When a new camera is introduced to the lab, the following definitions need to be updated:
- Camera enumeration
- How to distinguish that camera from others
- How to assign camera data into a tracker
